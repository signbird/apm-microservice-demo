server.port: 1888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/signbird/springcloud-config.git
          username: chinabaiqiufei@126.com
          password: demo!1234

eureka:
  instance:
    prefer-ip-address: true
#    instance-id: ${spring.application.name}:${server.port}
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
      
management:
  security:
    enabled: false